read_verilog RTL/register_comparator.v

hierarchy -top register_comparator

proc
opt
stat

select -module register_comparator
show -format svg -prefix synth/schematics/register_comparator

write_verilog synth/netlists/register_comparator_netlist.v