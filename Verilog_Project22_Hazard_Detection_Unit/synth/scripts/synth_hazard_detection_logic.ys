read_verilog RTL/hazard_detection_logic.v

hierarchy -top hazard_detection_logic

proc
opt
stat

select -module hazard_detection_logic
show -format svg -prefix synth/schematics/hazard_detection_logic

write_verilog synth/netlists/hazard_detection_logic_netlist.v