#=========================================================
# Project      : Verilog Project 06
# Design       : SPI Slave Shift Register
# Tool         : Yosys
#=========================================================

read_verilog RTL/spi_slave_shift_register.v

hierarchy -top spi_slave_shift_register

proc
opt

stat

show -format svg -prefix synth/schematics/spi_slave_shift_register

write_verilog synth/netlists/spi_slave_shift_register_netlist.v